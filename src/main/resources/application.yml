spring:
  application:
    name: school
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
---
spring:
  datasource:
    school:
#      jdbc-url: jdbc:h2:tcp://localhost:1521/test;MODE=LEGACY
      jdbc-url: jdbc:mysql://localhost:3306/school_db?useSSL=false&serverTimezone=UTC
      username: root
      password: root
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create
        show_sql: true
        format_sql: true